# Review

- 수정 요청 없으면 read-only로 진행함.
- finding 먼저 제시함.
- 실제 영향과 근거 중심으로 판단함.
- 취향을 결함으로 처리하지 않음.
- LOC·metric 하나로 결함 확정하지 않음.
- 기존 문제와 이번 변경으로 생긴 문제 구분함.
- 같은 원인의 문제는 하나로 묶음.
- 최소 수정 방향 제시함.
- 확인 못한 내용은 `미검증`으로 표시함.

우선 확인함:

```text
Security
→ Contract
→ Architecture
→ Data Integrity
→ Tests
```
